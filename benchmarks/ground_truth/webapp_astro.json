{
  "_note": "SFC fixture (Astro). Component apps don't carry the shared 13-scenario backend graph, so only scenarios 02 (file structure) and 08 (symbol search) are wired (see benchmarks/scenarios/02_*.sh and 08_*.sh). Values derived from real `cartog --json` output and hand-verified.",
  "02_file_structure": {
    "description": "What's in src/components/UserBadge.astro?",
    "query": "outline src/components/UserBadge.astro",
    "expected": [
      { "name": "AuthService", "kind": "import" },
      { "name": "Props", "kind": "interface" },
      { "name": "auth", "kind": "variable" },
      { "name": "describe", "kind": "function" },
      { "name": "label", "kind": "variable" }
    ]
  },
  "08_symbol_search": {
    "description": "Find all token-related symbols",
    "query": "search token",
    "expected": [
      { "name": "TokenError" },
      { "name": "generateToken" },
      { "name": "validateToken" },
      { "name": "issueDemoToken" },
      { "name": "TokenClaims" }
    ]
  }
}
