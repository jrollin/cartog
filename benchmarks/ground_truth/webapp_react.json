{
  "_note": "Component fixture (React TSX). Component apps don't carry the shared 13-scenario backend graph, so only scenarios 02 (file structure) and 08 (symbol search) are wired (see benchmarks/scenarios/02_*.sh and 08_*.sh). A React component is an ordinary `function` symbol — the `component` kind is SFC-only. Values derived from real `cartog --json` output and hand-verified.",
  "02_file_structure": {
    "description": "What's in src/components/LoginForm.tsx?",
    "query": "outline src/components/LoginForm.tsx",
    "expected": [
      { "name": "react", "kind": "import" },
      { "name": "../lib/auth", "kind": "import" },
      { "name": "LoginFormProps", "kind": "interface" },
      { "name": "LoginForm", "kind": "function" },
      { "name": "auth", "kind": "variable" },
      { "name": "onSubmit", "kind": "method" },
      { "name": "token", "kind": "variable" }
    ]
  },
  "08_symbol_search": {
    "description": "Find all token-related symbols",
    "query": "search token",
    "expected": [
      { "name": "TokenError" },
      { "name": "generateToken" },
      { "name": "validateToken" },
      { "name": "TokenClaims" }
    ]
  }
}
