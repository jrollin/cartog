\* TLC config for the PID-lock acquire model.
\* 3 concurrent acquirers exercise the three-way TOCTOU race
\* (one holds-then-dies, one reaps+wins, one acts on its stale read).
SPECIFICATION Spec

CONSTANTS Procs = {1, 2, 3}

INVARIANT Safe
