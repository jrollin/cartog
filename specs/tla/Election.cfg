\* TLC config for the single-writer election + promoter model.
SPECIFICATION Spec

CONSTANTS
  Procs = {1, 2, 3}
  MaxSchema = 2

\* The migration loop is unbounded; cap the schema counter so TLC terminates.
CONSTRAINT SchemaBound

INVARIANT Safe
